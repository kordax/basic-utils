# umath

Math helpers for common numeric operations.

Use this package for small generic math helpers that are not worth repeating in service code.

## Example

```go
min, max := umath.MinMax([]int{4, 1, 9})
avg := umath.Avg([]int{2, 4, 6})
```
